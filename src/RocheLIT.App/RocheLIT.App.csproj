<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <RootNamespace>RocheLIT</RootNamespace>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>

    <!-- Product metadata, surfaced in the executable and the installer. -->
    <AssemblyName>RocheLIT.App</AssemblyName>
    <AssemblyTitle>Roche LIT - Laboratory Interfacing Tool</AssemblyTitle>
    <Product>Roche LIT - Laboratory Interfacing Tool</Product>
    <Description>Laboratory Interfacing Tool</Description>
    <Company>Roche Diagnostics International</Company>
    <ApplicationIcon>Assets\LITAppIcon.ico</ApplicationIcon>
    <Version>1.0.27</Version>
    <AssemblyVersion>1.0.27.0</AssemblyVersion>
    <FileVersion>1.0.27.0</FileVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\RocheLIT.Core\RocheLIT.Core.csproj" />
  </ItemGroup>

  <!-- Bundle the Host Interface Manual next to the executable so the app ships
       with a default assay catalog. Linked from the repo root to avoid
       duplicating the PDF; SettingsLoader parses it at startup. -->
  <ItemGroup>
    <None Include="..\..\HIMv2_1.pdf" Link="HIMv2_1.pdf" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- Bundle brand assets (Roche logo) next to the executable; loaded at
       runtime by the toolbar. -->
  <ItemGroup>
    <None Include="Assets\**" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
